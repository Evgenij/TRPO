if (Connected == 0))
{
rez = setup();
fl_end = true; // выход
}
else
fl_end = true;

//---------------------------------------------

void Connecting()
{    
    while(true){}
    if (Connected == 0))
    {
        rez = setup();
        break;
    }
    else
        break;
}